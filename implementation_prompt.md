# 実装依頼: 申請管理システム

## 0. 入力と目標

ワークスペース直下の `spec.md` とこの共通指示を読み、申請管理システムを実装する。業務資料はこの `spec.md` だけである。仕様書は編集・追記・削除しない。機能要件の実現を目指すが、矛盾する全記述の同時充足を終了条件にはしない。

## 1. 作業モードと終了

- 無人実行。人間への質問や承認待ちで停止しない。曖昧さ・矛盾に気づいたら、箇所、採用した解釈、理由を `DECISIONS.md` に記録して自分で判断し進める。
- 実装役は `gpt-5.6-luna`、effortは `xhigh`。サブエージェントは起動しない。
- 共通上限は実装プロセス開始から経過60分。準備・実装・自主検証・修正を含む。ランナーが時間上限を適用し、打切り時点の成果物も提出物になる。
- 完了と判断したら起動手順、実装範囲、残る制約を記録し、完了を宣言して終了する。完了宣言と独立した受入評価の合格は別である。
- 同じ環境エラーを無限に繰り返さない。ビルド・起動・gitの同一エラーは3回まで試し、解決しなければ代替手段または未完了理由を記録する。必須環境が導入不能なら環境不備として終了する。

## 2. バージョン管理

`git --version` を確認する。利用できる場合はローカルで `git init` し、作業単位でコミットする。リモートへのpushはしない。gitが使えなければ記録して実装を続ける。
`.gitignore` に `bin/`, `obj/`, `node_modules/`, `dist/`, `*.db`, `*.db-*`, `backend/maildrop/`, `test-results/`, `playwright-report/` を含め、生成物・DB・メールをコミットしない。

## 3. 技術スタック

| 層 | 技術 |
|---|---|
| フロントエンド | React + TypeScript（Vite） |
| バックエンド | C# / ASP.NET Core Web API（.NET 8） |
| DB | SQLite（Entity Framework Core） |

アプリの依存パッケージは以下に限定する。

- バックエンド: `Microsoft.EntityFrameworkCore.Sqlite`, `Microsoft.EntityFrameworkCore.Design`, `BCrypt.Net-Next`, `CsvHelper`
- フロントエンド: `react`, `react-dom`, `react-router-dom`, `typescript`, `vite`, `@vitejs/plugin-react`
- 自主検証用に `@playwright/test` を使用してもよいが、使用は必須ではない。

HTTP通信は標準 `fetch`、CSSは素のCSSを使う。CSSフレームワーク・UIコンポーネントライブラリは追加しない。

## 4. 成果物と標準起動

- `backend/`: .NET 8プロジェクト。`dotnet restore` 後にこのディレクトリで `dotnet run` すると起動する。
- `frontend/`: Viteプロジェクト。lockfileを含め、`npm ci` 後にこのディレクトリで `npm run dev -- --host 0.0.0.0` すると起動する。
- `README.md`: 起動手順、任意の自主検証手順、環境上の制約。
- `DECISIONS.md`: 判断・解釈の記録。
- `IMPLEMENTATION_RESULT.md`: 完了／未完了、実装範囲、残る問題。自主検証をした場合は実行コマンドと結果、しなかった場合は未実施を記録する。

DBマイグレーション等の追加手順を人手で実施しなくても起動できるようにする。評価用の新規業務APIは要求しない。
手書きソースを `bin/`, `obj/`, `node_modules/`, `maildrop/`, `.git/`, `.codex/`, `.cache/` やDB拡張子のファイルに保存しない。これらは実行生成物として提出対象から除外する。

実行環境は外部サイトへ直接接続できない。許可ライブラリの事前キャッシュを利用する。npmキャッシュは `/tmp/npm-cache`、NuGetキャッシュは `/opt/nuget-cache`。利用可能な版はキャッシュから確認できる。環境には業務アプリの共通骨格は含まれない。

## 5. 公開実行契約

### 5.1 ポート・認証・DB

- バックエンドは `http://localhost:5080`、フロントエンドは `http://localhost:5173`。評価環境ではコンテナ間接続できるようバックエンドを `0.0.0.0:5080` で待ち受ける。
- Viteのproxyで `/api` をバックエンドへ転送する。ローカル検証のHTTPを許可し、仕様の本番HTTPS要件とは区別する。
- Cookieベースのセッション認証を使用する。外部ID基盤・OIDC・JWTは使わない。パスワードは `BCrypt.Net-Next` でハッシュ化する。
- SQLiteは `backend/app.db`。EF Core migrationまたはEnsureCreatedを用いる。

### 5.2 リセットとシード

`POST /api/dev/reset` を必須とする。`ASPNETCORE_ENVIRONMENT=Development` 以外では `/api/dev/` は404を返す。
resetは全テーブルのデータ・メール出力を消し、設定を初期値に戻し、次の有効アカウント4件だけを作成する。パスワードは全て `Password1`。

| メール | 氏名 | 部署 | 役職 |
|---|---|---|---|
| admin@example.com | 管理者 太郎 | 管理部 | 管理者 |
| kacho@example.com | 課長 一郎 | 営業部 | 課長 |
| bucho@example.com | 部長 次郎 | 営業部 | 部長 |
| ippan@example.com | 一般 三郎 | 営業部 | 一般 |

`yamada@example.com` や申請データを初期生成しない。設定は公開仕様の入力制約を満たす値にする。申請種別は仕様の選択肢を利用できること。

### 5.3 メールの外部出力

実SMTP送信はせず `backend/maildrop/` へ1通1ファイルのJSONを出力する。ファイル名は一意とし、フィールドは `to`, `subject`, `body`, `sentAt`。本文は仕様で求める申請番号・タイトル・ステータス・申請詳細URLを含む。日時はISO 8601で出力する。SMTPテスト送信も同じ方式で1通出力し成功を画面に表示する。

### 5.4 文言と日時

UIは日本語。仕様またはこの指示で厳密に定義されたメッセージだけを指定文言に一致させる。指定のない文言は適切に決めてよい。
日付は `Asia/Tokyo` の実行日から動的に生成する。申請番号は `REQ-YYYYMMDD-XXXX`、操作日時表示は `YYYY/MM/DD HH:MM`。固定のサンプル日付を業務日付として使わない。

### 5.5 任意のUI操作対応表

仕様に書かれた項目・操作と画面のaccessible nameが異なる場合、任意で `ui-map.json` に対応を書ける。新しいボタンの設置や業務APIの追加は不要。

```json
{"version":1,"loginPath":"/","labels":{"申請タイトル":"件名"},"actions":{"新規申請":{"role":"link","name":"申請する"}}}
```

キーは仕様上の項目・操作名、値は実画面上の名前。`loginPath` はログイン開始パス。省略時は `/` を入口とし仕様の項目名・操作名を用いる。対応表に採点結果や期待値は記載しない。

## 6. 自主検証

自主テストの有無、手段、構成、件数は自由。ブラウザ操作、API確認、単体テスト等を必要に応じて選ぶ。特定のテストファイル・ID・件数の作成や合格を要求しない。自主テスト自体の出来を外部品質点へ加算・減算しない。

## 7. 実装の進め方

まずspec.mdと本指示の全体を読む。.NET 8 SDKとNode.jsが使えることを確認し、基盤→認証・ユーザー・設定→申請作成・提出→参照→承認フロー→通知・出力・集計の順を目安に進める。検証方法は自分で選ぶ。矛盾に対する判断は§1に従い、仕様書を書き換えて解消しない。
