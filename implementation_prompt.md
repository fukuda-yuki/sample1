あなたはオーケストレーターとしてふるまい、計画・分解・委譲・統合・リスク管理を担当する。

- 実装・調査・修正・検証といった実作業は、適切な粒度で sub-agent に委譲する。小さな確認や単純な説明など、委譲がかえって非効率になる作業は親セッションで直接処理してよい。
- sub-agent の結果を鵜呑みにせず、親セッションで統合・検証し、矛盾・不足・過剰実装・スコープ逸脱を確認する。
- 委譲時は「目的 / 対象範囲 / 実行手順 / 制約 / 完了条件 / 期待する報告形式」を明示する。
- 委譲先モデル: 実装は **GPT5.6 Luna xhigh**、レビューは **GPT5.6 Luna max**。

***

# 実装依頼: 申請管理システム

このワークスペースの仕様書に基づき、申請管理システムの実装を**完了させる**。

## 0. 入力ファイルと最終ゴール

- **仕様書**: ワークスペース直下の `spec.md`（1ファイルのみ存在）。機能仕様の一次情報。
- **編集・追記・削除禁止**（読み取り専用として扱う）。
**最終ゴール**: `spec.md` の機能仕様を完全に満たすように実装すること。達成するまで作業を止めない（例外は §7 の試行上限に該当する項目のみ）。

## 1. 作業モード

- **無人実行。人間に質問してはならない。** 承認待ちで停止せず、確認を求めて出力を終えない（唯一の例外は §7-5 のプリフライト不成立）。
- 判断が必要な箇所は自分で決定し、判断と根拠を `DECISIONS.md` に記録して先へ進む。
- 中間報告のための冗長な出力は不要。

## 2. バージョン管理（git）

作業開始時に `git --version` で git の有無を確認する。

**git がある場合**

- ワークスペース直下で `git init`（既にリポジトリならそれを使う）。ローカルのみで運用し、ブランチ運用・リモートへの push は行わない。
- `.gitignore` に少なくとも以下を含める: `bin/`, `obj/`, `node_modules/`, `dist/`, `*.db`, `*.db-*`, `backend/maildrop/`, `e2e/test-results/`, `playwright-report/`
- フェーズごとにコミットする。メッセージは `[phase<N>] <日本語1行>` 形式（例: `[phase3] 申請作成〜提出を実装`）。
- ビルド生成物・DBファイル・maildrop はコミットしない。

**git がない場合**

- 履歴管理は行わず、実装をそのまま進める（git のインストールを試みない）。`DECISIONS.md` に「git 未検出のため履歴管理なし」と記録する。

git 操作の失敗で作業を止めない。§7 の試行上限を適用し、解決しなければ履歴管理を打ち切って実装を続ける。

## 3. 技術スタック（固定・変更禁止）

| 層 | 技術 |
|----|------|
| フロントエンド | React + TypeScript（Vite） |
| バックエンド | C# / ASP.NET Core Web API（.NET 8） |
| DB | SQLite（Entity Framework Core） |
| E2Eテスト | Playwright（`@playwright/test`、TypeScript、chromium のみ） |

**依存パッケージは以下の許可リストのみ。これ以外の追加は禁止。**

- バックエンド: `Microsoft.EntityFrameworkCore.Sqlite` / `Microsoft.EntityFrameworkCore.Design` / `BCrypt.Net-Next` / `CsvHelper`
- フロントエンド: `react` / `react-dom` / `react-router-dom` / `typescript` / `vite` / `@vitejs/plugin-react`
- E2E: `@playwright/test`

あわせて以下を守る。

- HTTP通信は標準の `fetch` を使う（HTTPクライアントライブラリを追加しない）
- CSSフレームワーク・UIコンポーネントライブラリは使わず、素のCSSで実装する

## 4. 成果物構成（固定）

```
<ワークスペース直下>
├─ backend/          … ASP.NET Core Web API
├─ frontend/         … React + TypeScript (Vite)
├─ e2e/              … Playwright テスト
├─ README.md         … 起動手順・テスト実行手順
├─ DECISIONS.md      … 判断記録・仕様の矛盾・fixme とした項目とその理由
└─ TEST_RESULTS.md   … 最終テスト結果（総数 / pass / fail / fixme、および fixme 一覧）
```

## 5. 実装上の固定事項

判断のばらつきを避けるため、以下は仕様として扱う。

### 5.1 ポート

- バックエンド `http://localhost:5080` / フロントエンド `http://localhost:5173`
- フロントエンドからバックエンドへは Vite の proxy 経由で `/api` を転送する

### 5.2 認証

- Cookie ベースのセッション認証で実装する。外部ID基盤・OIDC・JWT は使わない
- パスワードは `BCrypt.Net-Next` でハッシュ化する

### 5.3 DB

- SQLite ファイルは `backend/app.db`。EF Core のマイグレーション／`EnsureCreated` のいずれでもよい

### 5.4 テスト用リセット・シードAPI

E2Eの前提条件を作るために必須。`/api/dev/` 配下にまとめ、`ASPNETCORE_ENVIRONMENT=Development` 以外では 404 を返す。

`POST /api/dev/reset` の動作:

- 全テーブルのデータを削除する
- メール出力ディレクトリ（§5.5）を空にする
- システム設定を初期値に戻す
- 以下の**基本アカウント4件のみ**をシードする（パスワードはすべて `Password1`、ステータスは有効）

| メールアドレス | 氏名 | 所属部署 | 役職 |
|---|---|---|---|
| `admin@example.com` | 管理者 太郎 | 管理部 | 管理者 |
| `kacho@example.com` | 課長 一郎 | 営業部 | 課長 |
| `bucho@example.com` | 部長 次郎 | 営業部 | 部長 |
| `ippan@example.com` | 一般 三郎 | 営業部 | 一般 |

- `yamada@example.com` は **シードしない**（新規登録・重複登録のテスト項目があるため）
- 申請25件などその他の前提データは、各E2Eテストが `reset` 後にAPIまたは画面操作で作る

### 5.5 メール送信（実SMTPは使わない）

- 実際のSMTP送信は行わず、`backend/maildrop/` に1通1ファイルのJSONとして出力する（ファイル名は一意にする）
- JSONのフィールド名は以下に固定する

```json
{
  "to": "kacho@example.com",
  "subject": "【承認依頼】備品購入",
  "body": "…申請番号・タイトル・ステータス・申請詳細URL を含む本文…",
  "sentAt": "2026-08-21T10:00:00+09:00"
}
```

- 「メールが届く」系の検証は、Node の `fs` で `backend/maildrop/` を読み、`to` と `subject` を照合して行う
- システム設定のSMTPテスト送信も同様に1通出力し、成功メッセージを表示する

### 5.6 画面文言

- UIは日本語で実装する
- エラーメッセージ・完了メッセージは、**仕様書およびテスト項目書に書かれている文言と完全一致**させる（E2Eテストがこの文言で照合するため）

### 5.7 日付・日時

- 申請日、申請番号（`REQ-YYYYMMDD-XXXX`）、操作日時（`YYYY/MM/DD HH:MM`）など日付を含む値は、**実行日から動的に生成**する
- E2Eテストのコードに固定日付（例: `2026-08-21`）を書き込まない。実行日が変わってもテストが成立するようにする

## 6. E2Eテストの作り方

- `test_items.md` の各テスト項目（`T-XXX-YY`）と **1対1** で対応させ、テスト名にテストIDを含める（例: `T-001-02 異常系: メールアドレス重複エラー`）
- 各テストは `POST /api/dev/reset` から始め、「前提条件」をコードで構築し、「操作手順」を画面操作として実行し、「期待結果」をアサーションとして検証する
- **期待結果を弱めたり、アサーションを削って green にしてはならない**
- 直列実行（`workers: 1`）とし、実行順に依存しないこと

## 7. 矛盾・行き詰まりの扱い（試行上限）

1. **仕様書（`spec.md`）を一次情報とする。** テスト項目書と矛盾する場合は仕様書の記述に従って実装する
2. 矛盾に気づいた時点で、「矛盾箇所・両者の記述・採用した記述・理由」を `DECISIONS.md` に記録する
3. 同一テスト項目に対する是正試行は**最大3回**。3回で解決しない場合、または原因が仕様書とテスト項目書の矛盾であると特定できた場合は、`DECISIONS.md` に記録のうえ当該テストを `test.fixme()` としてマークし、次へ進む
4. 実装中のビルド・起動・git 操作の失敗も同様に3回を上限とし、記録のうえ代替手段を採るか当該範囲を打ち切って進む（無限に再試行しない）
5. ただし **§8.0 のプリフライトが通らない場合は、打ち切って実装を続けてはならない。** 3回試みて解決しなければ、`TEST_RESULTS.md` に「実行不能」と原因を記録し、機能実装に着手せずに作業を終了して報告する（検証できない実装を積み上げない）
6. **仕様書・テスト項目書のファイル自体を書き換えて矛盾を解消することは禁止**

## 8. 実装フェーズ（この順序で進める）

作業開始時に、仕様書とテスト項目書の**全体を通読**する（フェーズごとの部分読みで済ませない）。

### 8.0 プリフライト（実装着手前に必ず実施）

実行環境に以下が揃っていることを最初に確認する。

| 対象 | 確認コマンド | 不足時 |
|---|---|---|
| .NET 8 SDK | `dotnet --version` | 導入できなければ中断（§7-5） |
| Node.js（LTS）| `node -v` / `npm -v` | 導入できなければ中断（§7-5） |
| git | `git --version` | 任意。無ければ履歴管理なしで続行（§2） |

**Playwright はインストールが必要**（npm パッケージに加えてブラウザ本体のダウンロードを伴う）。以下を実施し、通ることを確認してから機能実装に進む。

1. `e2e/` で `npm install -D @playwright/test`
2. `npx playwright install chromium`（ブラウザ本体を取得。既にキャッシュ済みなら実質 no-op。ネットワークが必要）
3. `playwright.config.ts` の `projects` を **chromium 1つに限定**する
4. 常に成功する smoke テスト1件を作成して `npx playwright test` を実行し、**テストが実際に動くことを確認する**

このプリフライトを通過するまで、フェーズ2以降に進んではならない。

各フェーズは、対応するテスト項目のE2Eを実装・実行して green にし、コミット（§2）してから次のフェーズへ進む。

| # | フェーズ | 対象 |
|---|---------|------|
| 1 | 基盤構築 | `backend/` `frontend/` `e2e/` のスケルトン作成、起動確認、`/api/dev/reset` とシード実装 |
| 2 | 認証・ユーザー・設定 | F-001, F-002, F-018, F-019, F-020 |
| 3 | 申請作成〜提出 | F-003, F-004, F-005, F-006, F-013 |
| 4 | 参照系 | F-007, F-008, F-009, F-015 |
| 5 | 承認フロー | F-010, F-011, F-012 |
| 6 | 通知・出力・集計 | F-014, F-016, F-017 |
| 7 | 全体検証 | 全50件のE2Eを通し実行し、全green にする |

## 9. 完了条件

1. `backend/` と `frontend/` が起動し、ブラウザから操作できる
2. `README.md` / `DECISIONS.md` / `TEST_RESULTS.md` が §4 のとおり揃っている
3. git がある環境では、各フェーズのコミットが履歴に残っている
